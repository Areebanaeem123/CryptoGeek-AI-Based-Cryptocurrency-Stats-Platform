# 03 AI Embeddings

This component handles the **Local AI Embedding Service**.

## Features
- Loads the `sentence-transformers` model (`all-MiniLM-L6-v2`).
- Converts news articles and user queries into high-dimensional numerical vectors.
- Runs entirely locally for privacy and speed.
