# 02 DB Storage

This component manages the **Relational Persistence** layer of the system.

## Features
- Defines the database schema using **SQLAlchemy** models (Coins, News, Market Data).
- Manages database connections and asynchronous sessions.
- Handles migrations through **Alembic**.
