# Core Configuration

Shared settings and utilities for the entire system.

## Features
- Centralized `config.py` using `pydantic-settings`.
- Environment variable management via `.env`.
- Project-wide logging configuration.
